# EditorConfig：统一各编辑器的缩进与换行规范
root = true

[*]
charset = utf-8
indent_style = space
indent_size = 4
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# Markdown 尾部空格是硬换行，不裁剪
[*.md]
trim_trailing_whitespace = false

# Makefile 必须使用 Tab
[Makefile]
indent_style = tab
