候选人
我们一开始去全量请求的模板，然后大概有一千多个模板数据，就是这一千个的模板数据可能达到了二三十兆这个样子。

面试官
OK 如果现在让你有机会专门要处理这一块的优化，这个节省你有什么办法控制这一块的一个性能？专门去处理这个json。你可以想好的告诉我最终的可以哪些技术手段。

候选人   15:37

我理解就是首屏数据接送太大，应该怎么去处理对吧？也不一定是体积的问题。对，还有，可是客服区也不一定是体积，可能是多方面的。

面试官  15:49

你好好想想这种长json怎么办？

候选人 15:50
我觉得可以拆。就比如说我们很多比较慢的页面，比较拖节奏的页面。其实是那种有长达 150 多个楼层的，或者说 100 个楼层以上的那其实我们即便是 B 端的页面，我们在一开始初始化的时候是不需要渲染一百多个楼层的那我是不是可以进行一个首屏的一个拆解。比如说我每次只先加载十个楼层，然后等到页面基本上稳定下来之后，然后再去把后面的数据逐层加载下来。这样是不是可以实现一个优化，相当于是把一，你讲这个你得试一试。对，这是我想到的一个点。相当于是把一份页面那个结算数据不是一口气把它拉下来，而是去分段把它请求下来。

候选人   16:51

还有我想一想，如果是因为计算逻辑导致 GS 阻塞的话，我在想是否可以去使用 web worker 这样的另外一个线程来去做一计算和处理。为什么 work 这块？因为它不占用那个浏览器的渲染主线程。如果兜底和数据那个数据做好的话，其实浏览器会继续往后渲染它相对应的组件。然后你相当于是把这个计算的逻辑，多余的计算逻辑交给另外一个线程去处理，然后等它计算完之后，你再从别的县城去拿。

面试官
所以在浏览器里面就一定会被 block 住吗？您再重复一遍，block 就是说所以你在浏览器里面就一定要用主线程渲染，就一定会被阻塞在这个地方了。

面试官   17:46

浏览器有没有什么办法也可以让他进行异步的不渲染？

候选人
有可以用。比如说像 set time out，比如说就 set time out，它也是加入到另在一个时间队列里面去。然后比如说网络你计算的这个过程你是变不了的，你还是要占用你的算力，你就是也他这个 LS 进行计算对吧？那你的主某一次计算的结果你就依赖它。对，是的。好吧，二倍知道你这块的深度了。然后剩下的两个有没有新地方还是值得可以聊一聊的。
